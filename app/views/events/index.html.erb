Here is a list of our workshops:
<ul>
  <% @events.each do |event| %>
  <li>
    <%= link_to event.name , new_event_application_path(event) %>
    <%= l event.scheduled_at %>
  </li>
  <%end %>
</ul>

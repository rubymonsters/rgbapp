<table>
  <thead>
    <tr>
      <th>Name</th>
      <th>Date</th>
      <th></th>
    </tr>
  </thead>
  <tbody>
    <% @events.each do |event| %>
    <tr>
      <td>
        <%= event.name %>
      </td>
      <td>
        <%= l event.scheduled_at %>
      </td>
      <td class="actions">
        <%= link_to 'Applicants', admin_event_applications_path(event) %>
        <%= link_to 'Attendees', admin_event_attendants_path(event) %>
        <%= link_to 'Coaches', admin_event_coach_applications_path(event) %>
        <%= link_to 'Groups', admin_event_groups_path(event) %>
        <%= link_to 'Edit', edit_admin_event_path(event) %>
        <%= button_to 'Delete', admin_event_path(event), method: :delete, data: { confirm: "Are you sure?" } %>
      </td>
  </tr>
    <%end %>
  </tbody>
</table>

<p><%= button_to 'Add event', new_admin_event_path, method: :get  %></p>
